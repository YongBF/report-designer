# Code Review Command

智能代码审查，检查代码质量、安全漏洞、性能问题和最佳实践。

## 使用方法

### 审查当前修改
```bash
@review
```

### 审查特定文件
```bash
@review src/components/Button.vue
```

### 审查整个项目
```bash
@review --all
```

### 只检查安全问题
```bash
@review --security
```

### 只检查性能问题
```bash
@review --performance
```

## 审查维度

### 1. 代码质量
- 命名规范
- 代码复杂度
- 代码重复
- 函数长度
- 注释质量

### 2. 安全性
- SQL 注入
- XSS 漏洞
- CSRF 保护
- 敏感数据泄露
- 依赖漏洞

### 3. 性能
- 算法复杂度
- 内存泄漏
- 不必要的渲染
- 资源加载
- 缓存策略

### 4. 最佳实践
- 框架规范
- 设计模式
- 错误处理
- 类型安全
- 测试覆盖

## 输出格式

```markdown
# Code Review 报告

## 总体评分
- 代码质量: ⭐⭐⭐⭐☆
- 安全性: ⭐⭐⭐⭐⭐
- 性能: ⭐⭐⭐☆☆
- 最佳实践: ⭐⭐⭐⭐☆

## 问题清单

### 🔴 严重问题 (必须修复)
1. [src/utils/helper.ts:45] SQL 注入风险
   ```typescript
   const query = `SELECT * FROM users WHERE id = ${userId}`;
   ```
   **建议**: 使用参数化查询
   ```typescript
   const query = 'SELECT * FROM users WHERE id = ?';
   db.query(query, [userId]);
   ```

### 🟡 警告 (建议修复)
2. [src/components/App.vue:120] 未使用的变量
   ```typescript
   const unused = getData();
   ```
   **建议**: 移除未使用的代码

### 🔵 优化建议 (可选)
3. [src/services/api.ts:50] 可以使用 async/await
   ```typescript
   .then(data => processData(data))
   ```
   **建议**:
   ```typescript
   const data = await fetchData();
   return processData(data);
   ```

## 最佳实践建议
- 使用 TypeScript 严格模式
- 添加单元测试覆盖核心逻辑
- 使用 ESLint + Prettier 统一代码风格

## 改进优先级
1. 🔴 修复严重安全问题
2. 🟡 改进代码质量
3. 🔵 优化性能
```

## 集成工具

- ESLint
- Prettier
- SonarQube
- CodeClimate
- GitHub Actions
